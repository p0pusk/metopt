Функция:

f(x) = a * x_1 + x_2 + 4 * sqrt(1 + b * x_1 ** 2 + c * x_2 ** 2)
a = 2
b = 5
c = 3

Задача:

1) Градиентный метод первого порядка наискорейшего
спуска (шаг определяется по методу ЗС и пробных точек). Рисует линии
уровня функции цели и показывает в ходе вычислений
эксперимента ортогональность звеньев градиентной ломаной на
двух последовательных итерационных шагах (для
точности eps = 0.01).Рисует ее.

2) Градиентный метод второго порядка ДФП

3) Сравнительный анализ алгоритмов методов.
